var users = ['ali', 'raza', 'umar', 'hassan', 'admin'];
if (users.length < 0) {
    console.log('We need more users.');
}
users = [];
console.log("All users have been removed, ".concat(users.length, ", We need to find some users"));
